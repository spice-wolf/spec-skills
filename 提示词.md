# 提示词

## 初始化项目

帮我创建一个名字为 init-spec 的 Agent Skill 用于初始化基于 DDD 和 Java 开发的项目的 Specification（规范），这些 Specification 将用于后续基于规范驱动开发（SDD，即Spec-Driven Development）时的上下文输入，但是现阶段我只需要这个 Skill 能够正确地初始化项目的 Specification

项目的 Specification 由多份 Specification 文件（markdown 格式的文件）组成，这些 Specification 文件从项目的不同层面描述项目，并且存放在不同的文件目录层级。init-spec 在初始化项目的 Specification 文件时应该按照以下目录层级输出对应的 Specification 文件：

1. `${specification-root}/constitution/constitution.md`：这份 Specification 文件是项目的宪法文件，用于描述在开发时 AI 永远需要遵守的事项。例如：项目的框架必须使用 xx，项目的分层结构必须遵守 DDD、永远不要使用 `@Autowired` 注解等等（这些只是方便你理解的例子，不要写死在 Skill 中）。`constitution.md` 的内容应该尽可能简短，其中的条目最好不要超过 10 条
2. `${specification-root}/architecture/architecture.md`：这份  Specification 文件是项目的整体架构文件，里面的内容应该包含：简述项目的开发框架、简述项目的分层结构（如果项目使用 DDD 则描述哪些模块对应 DDD 的哪一层级）、简述项目引入的 Git 子模块（如果有的话）。`architecture.md` 需要简要地描述项目的整体架构，而不是描述具体的功能流程图
3. `${specification-root}/architecture/DDD.md`：可选的，如果项目使用了 DDD（领域驱动开发），则生成这份  Specification 文件用于描述 DDD 中必须要遵守的事项以及项目当前的 DDD 实施情况（项目具体的分层、聚合根、实体等等）
4. `${specification-root}/domain/${specific-domain}.md`：如果项目使用了 DDD 或者具备明显的 DDD 的特点，则生成这份  Specification 文件用于描述项目中包含的各个领域（domain）。注意：`${specific-domain}` 指这份文件描述的具体领域的名称，例如 `merchant.md`（商户领域）、`terminal.md`（终端领域）等等，在创建文件时应该换成具体领域的名称。一个项目中可能会包含多个领域，因此需要在 `${specification-root}/domain/` 目录下创建多份 Specification 文件用于描述所有领域。项目的领域通常包含在 DDD 分层中的 domain 层，并且不同领域的实体包含在不同文件夹下
5. `${specification-root}/tool/tool.md`：这份  Specification 文件描述项目中常用的工具类或者工具方法，这些工具类或者工具方法的明显特点是：专门解决某一类场景问题（例如参数校验、或者链路 ID 等等），并且在代码中被多次调用。需要识别出这些常用的工具类或者工具方法，并且描述它们的应用场景，什么时候应该优先调用它们。注意：这些工具类或工具方法可能是开发者在项目中编写的，也可能是第三方依赖引入的

`${specification-root}` 是项目 Specification 文件的根目录，init-spec 应该首先询问用户根目录是哪个目录，你可以建议用户使用默认目录 `./specification`（即在项目根目录下创建一个名称为 `specification` 的目录作为根目录）

init-spec 内部规定一个项目的领域如果超过 3 个则为大型项目，如果小于等于 3 个则为小型项目。对于大型项目，init-spec 应该分步骤分别初始化上述 Specification 文件，以免 AI 上下文过长。对于小型项目，init-spec 可以一次性初始化上述所有的 Specification 文件。在识别出项目是否为大型项目之后，init-spec 应该提示用户选择分步骤初始化还是一次性初始化，并且基于当前项目的类型给出建议

如果是分步骤初始化 Specification 文件，init-spec 应该生成一个 Todo List 记录哪些 Specification 文件已经生成，哪些 Specification 文件还未生成，并且在每生成一份 Specification 文件之后更新 Todo List





很好，但是我需要你优化一下 Specification Template，我需要 Specification 文件在后续能够以渐进式披露的方式（类似 Agent Skill 的工作原理）提交给 AI 作为上下文以进行规范驱动开发，所以我需要在这些 Specification 文件中都加上类似于 Agent Skill 的元数据描述，简要描述在何时应该这份 Specification 文件作为 AI 的上下文。我需要你优化一下 Specification Template 以满足这个需求





很好，现在我需要实现渐进式披露 Specification 文件的方法。例如，当我向 AI 提问“帮我完成‘新增商户审核’功能的需求”时，我需要 AI 能够正确地知道先去读取 `${specification-root}/constitution/constitution.md` 文件和 `${specification-root}/domain/merchant.md` 文件作为上下文，然后再去帮我完成这个需求，因为 `constitution.md` 文件是项目的宪法文件每次都必须作为上下文，`merchant.md` 是商户领域 Specification 和我的提问相关。我有什么好的办法可以实现这一点吗？你先给出建议供我选择而不用去实现它



很好，我很喜欢你给出的“Manifest 索引路由 + constitution 必载 + 两阶段执行”的方式。现在我需要你帮 2 个新的 Skill 用于完成这项事情。

第一个 Skill 的名称叫做 indexing-spec，它聚焦于维护“Manifest 索引路由”。它的作用是用于维护本项目的所有 Specification 文件的索引文件，这个索引文件存放于 `${specification-root}/index.md`，索引文件中包括所有 Specification 文件（但不包括 `index.md` 文件本身）的 Manifest 信息，包括：

- path
- name
- description
- always_load
- domains
- triggers
- priority

目的是让 AI 读取 `index.md` 文件之后能够知道自己接下来要读取哪些 Specification 文件来完成用户的任务。每个 Specification 文件的 Manifest 信息用 JSON 形式进行描述。

indexing-spec 的调用时机是在项目的 Specification 文件被创建或被修改时（特别是 Specification 文件的 YAML frontmatter 被修改时，这意味着这份文件的读取时机可能发生了变化）。并且，最好只针对被创建或者修改的 Specification 文件更新其索引，而不是每次都针对项目所有  Specification 文件全量更新索引。

第二个 Skill 的名称叫做 retrieval-spec，它聚焦于“constitution 必载 + 两阶段执行”。它的作用是让 AI 先读取 `${specification-root}/index.md`文件，然后执行最小加载策略加载具体的 Specification 文件作为会话上下文来解决用户的问题。

另外，我需要你实现最小加载策略，针对 SDD 场景至少分 3 层：

- L0 必载
  - constitution/constitution.md
- L1 按任务类型加载
  - 架构类任务：architecture/architecture.md
  - DDD 建模类任务：architecture/DDD.md
  - 工具复用类任务：tool/tool.md
- L2 按领域加载
  - domain/merchant.md
  - domain/terminal.md
  - 其他具体领域文件

你可以自行判断在 indexing-spec 中实现这个功能还是在 retrieval-spec 中实现这个功能。

